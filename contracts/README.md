## Integration Contracts

- MINDEX client currently targets contract version `v1alpha` (see `mycosoft_mas/integrations/clients/mindex_client.py`).
- Add future OpenAPI/JSON schema pins here to keep integrations versioned.
